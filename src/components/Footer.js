import React from 'react'

const Footer = props => {

    return <div style={{ background: "#000080", width: "100vw", color: "white", height: "50vh", justifyContent: "center", alignItems: "center", display: "flex" }}>
        This is a dummy footer I will make a better one in future
    </div>
}

export default Footer;